let shape = /* create your shape here */

// Set the stroke color to red and the width to 2 points
shape.stroke(color: .red, with: .style(width: 2))

// You can also specify additional style properties if supported
shape.stroke(color: .red, with: .style(width: 2, lineCap: .round, lineJoin: .bevel))
